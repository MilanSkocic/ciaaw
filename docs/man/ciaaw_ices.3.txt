ciaaw_ices(3)                Library calls                ciaaw_ices(3)

NAME
       ices - get the isotopic compositions

LIBRARY
       ciaaw library (libciaaw, -lciaaw)

SYNOPSIS
       Fortran   function ice(s)result(res)

                    character(len=*), intent(in)  :: s

                    real(dp), pointer  :: res(:,:)

       C         double *ciaaw_ices(char *s,int n);

DESCRIPTION
       Get all the isotopic compositions of the element s.

       Arguments:

       s         Element symbol.

RETURN VALUE
       (n,3)array
                 Fortran order array with the mass number, the composi‐
                 tion and the uncertainty for the n isotopes.

       NULL      If the provided element is incorrect.

SEE ALSO
       ciaaw(3),    ciaaw_cli(1),    ciaaw_version(3),    ciaaw_saw(3),
       ciaaw_ice(3),   ciaaw_nice(3),   ciaaw_naw(3),    ciaaw_nnaw(3),
       ciaaw_ices(3), ciaaw_naws(3), ciaaw_s2z(3)

1.3.2                        30 August 2026               ciaaw_ices(3)
